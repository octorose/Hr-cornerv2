import KSUID from "ksuid";

export const generateId = () => {
  const id: any = Math.floor(Math.random() * 99999999999);
  console.log(id.toString());
  
  return id.totring;
};
export const CandidatIdGenerator = () => {
  const id = "Pend_hst";

  return id;
};
